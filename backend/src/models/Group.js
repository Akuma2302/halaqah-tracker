/**
 * `groups` (accountability groups) + `group_members` join table.
 * @typedef {Object} Group
 * @property {string} id
 * @property {string} name
 * @property {string} owner_id
 * @property {string} invite_code
 * @property {string} created_at
 * @property {string} updated_at
 */
module.exports = {};
