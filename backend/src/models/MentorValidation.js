/**
 * `mentor_validations` — one row per user per week, self-ticked.
 * @typedef {Object} MentorValidation
 * @property {string} id
 * @property {string} user_id
 * @property {string} week_start
 * @property {boolean} is_validated
 * @property {string} validated_date
 */
module.exports = {};
