# redux/

This project currently manages state with React Context (`src/context`) and
local component state, so this folder is empty. It's kept as a placeholder
matching the standard frontend structure — if the app grows enough to need
global state management beyond Context, Redux Toolkit slices would live here.
